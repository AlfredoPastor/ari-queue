package application

import "testing"

func Test_Creator_Agent(t *testing.T) {
	tests := []struct {
		name string
		want error
	}{}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			//assert.Equal(t, tt.want, got, "Los errores son diferentes")
		})
	}
}
